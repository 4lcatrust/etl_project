[pytest]
testpaths = tests
# Put the DAG folder on sys.path so `import module.*` resolves the same way Airflow does.
pythonpath = dags
markers =
    airflow: requires apache-airflow installed (DAG parsing / operators)
addopts = -ra
